/**
 * Defines the breakpoints for mobile, tablet, and desktop screens.
 */
const breakpoints = Object.freeze({
  mobile: {
    min: 320,
    max: 767,
  },
  tablet: {
    min: 768,
    max: 1023,
  },
  desktop: {
    min: 1024,
  },
});

export default breakpoints;
